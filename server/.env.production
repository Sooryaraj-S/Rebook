# Production Environment Variables
# These should be set in your hosting platform (Heroku config vars, DigitalOcean env, etc.)
# DO NOT commit actual values - this is a template only

# MongoDB Atlas Connection (with URL-encoded password if needed)
MONGODB_URI=mongodb+srv://Sooryaraj_bright:PASSWORD_HERE@rebookusers.8xpvxko.mongodb.net/rebook?appName=Rebookusers

# JWT Secret - Use a strong random string (32+ characters)
JWT_SECRET=your_super_secret_jwt_key_minimum_32_chars_recommended_change_this_12345678

# Server Port (usually auto-assigned by hosting provider)
PORT=5000

# Environment
NODE_ENV=production

# Optional: Client URL for CORS (if different from hosting domain)
CLIENT_URL=https://your-domain.com
